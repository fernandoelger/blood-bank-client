import { AverageImcByAgeInterval } from './average-imc-by-age-interval';

describe('AverageImcByAgeInterval', () => {
  it('should create an instance', () => {
    expect(new AverageImcByAgeInterval()).toBeTruthy();
  });
});
