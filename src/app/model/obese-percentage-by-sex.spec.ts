import { ObesePercentageBySex } from './obese-percentage-by-sex';

describe('ObesePercentageBySex', () => {
  it('should create an instance', () => {
    expect(new ObesePercentageBySex()).toBeTruthy();
  });
});
