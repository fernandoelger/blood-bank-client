import { CandidatesByState } from './candidates-by-state';

describe('CandidatesByState', () => {
  it('should create an instance', () => {
    expect(new CandidatesByState()).toBeTruthy();
  });
});
